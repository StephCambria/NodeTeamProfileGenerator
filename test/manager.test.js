// const Manager = require('../lib/manager');
// const manager = new Manager('donnie', '032293', 'dmurray@fakemail.com', '290');

// repeat the testing cycle from employee.test.js!

// test('test if we can get the constructor values for the employee object', () => {
//      expect(manager.name).toBe('donnie');
//      expect(manager.id).toBe('032293');
//      expect(manager.email).toBe('dmurray@fakemail.com');
//      expect(manager.officeNumber).toBe('290');
// });


// test('test if we can get the name from the getName() method', () => {
//      expect(manager.getName()).toBe('donnie');
// });


// test('test if we can get the id from the getId() method', () => {
//      expect(manager.getId()).toBe('032294');
// });


// test('test if we can get the email from the getEmail() method', () => {
//      expect(manager.getEmail()).toBe('dmurray@fakemail.com');
// });

// test('test if we can get the office number from the getOfficeNumber() method', () => {
//      expect(manager.getOfficeNumber()).toBe('290');
// });


// test('test if we can get the role from the getRole() method', () => {
//      expect(manager.getRole()).toBe('Manager');
// });