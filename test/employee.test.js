// use jest to run testing
// create lib folder in directory later!

// =====================================================

// const Employee = require('../lib/employee);
// const employee = new Employee('kristynna', '100297', 'kmvaldes@fakemail.com);

// =====================================================

// test('test if we can get the constructor values for the employee object', () => {
//      expect(employee.name).toBe('kristynna');
//      expect(employee.id).toBe('100297');
//      expect(employee.email).toBe('kmvaldes@fakemail.com');
// });


// test('test if we can get the name from the getName() method', () => {
//      expect(employee.getName()).toBe('kristynna');
// });


// test('test if we can get the id from the getId() method', () => {
//      expect(employee.getId()).toBe('100297');
// });


// test('test if we can get the email from the getEmail() method', () => {
//      expect(employee.getEmail()).toBe('kmvaldes@fakemail.com');
// });


// test('test if we can get the role from the getRole() method', () => {
//      expect(employee.getRole()).toBe('Employee');
// });